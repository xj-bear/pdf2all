# PDF2All MCP Server

**PDF2All** 是一个功能强大的 Model Context Protocol (MCP) 服务器，旨在为大语言模型（LLM）提供全方位的 PDF 转换能力。它集成了多种转换引擎，支持将 PDF 文档转换为可编辑的 Excel、Word、PPT 文件以及图像格式。

## 🌟 主要功能

- **PDF 转 Excel (`pdf_to_excel`)**:
  - 智能提取表格数据。
  - **OCR 支持**: 针对扫描件，支持使用 OCR 技术（基于 RapidOCR）提取表格。
  - **并行处理**: 多进程并发处理，大幅提升大文件转换速度。
  - **页面选择**: 支持指定转换特定页面。

- **PDF 转 Word (`pdf_to_docx`)**:
  - 高保真还原文档布局、字体和图片。
  - 支持快速模式。

- **PDF 转 PPT (`pdf_to_ppt`)**:
  - 将 PDF 页面转换为 PowerPoint 幻灯片。
  - 适合制作演示文稿。

- **PDF 转 图片 (`pdf_to_jpg`)**:
  - 将 PDF 页面转换为高清 JPG 图片。
  - 支持自定义 DPI 和压缩质量。

- **SSE 支持与文件下载**:
  - 支持 Server-Sent Events (SSE) 传输协议。
  - 提供生成的文件的 HTTP 下载链接，方便本地保存。

## 🚀 快速开始 (npx)

无需安装，直接使用 `npx` 运行：

```bash
npx pdf2all-mcp
```

或者启动 SSE 模式（推荐用于 Cherry Studio 等支持 SSE 的客户端）：

```bash
npx pdf2all-mcp --sse
```

## 📦 安装与配置

### 前置要求

在使用本工具之前，请确保您的系统已安装：

1.  **Node.js** (>= 18.0.0)
2.  **Python** (>= 3.8)
3.  **Python 依赖库**:
    本工具依赖 Python 脚本进行转换。请确保安装了以下 Python 库：

    ```bash
    pip install pdf2docx pdfplumber pandas pymupdf python-pptx rapidocr_onnxruntime openpyxl requests
    ```

### Claude Desktop 配置

在您的 `claude_desktop_config.json` 中添加以下配置：

```json
{
  "mcpServers": {
    "pdf2all": {
      "command": "npx",
      "args": ["-y", "pdf2all-mcp", "--sse"]
    }
  }
}
```

### Cherry Studio 配置

- **命令**: `npx`
- **参数**: `["-y", "pdf2all-mcp", "--sse"]`

## 🛠️ 开发

```bash
# 安装依赖
npm install

# 构建
npm run build

# 运行 (Stdio 模式)
npm start

# 运行 (SSE 模式)
npm run start:sse
```

## 📄 许可证

MIT
